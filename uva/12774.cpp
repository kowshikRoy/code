/**
 * Author: Repon Kumar Roy
 * Date: 2021-04-25
 * Task: 12774
 */

#include <bits/stdc++.h>
using namespace std;

#define ll        long long
#define REP(i, n) for (int i = 0; i < (n); i++)

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  int t, cs = 0;
  cin >> t;
  while (t--) {
    int n, m, k;
    cin >> n >> m >> k;
    vector<int> u(m), v(m), d(m), w(m);
    REP(i, m) { cin >> u[i] >> v[i] >> d[i] >> w[i]; }
  }
  return 0;
}
